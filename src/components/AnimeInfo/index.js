import React, {/* useState,useEffect, */ useContext, Fragment} from 'react';
// import {useAnimeInfo} from '@hooks/useAnimeInfo'
// import { useListOfCapitules } from '@hooks/useListOfCapitules'

export const AnimeInfo = ({params}) => {
    console.log(params)
    return ( 
    <Fragment>
        <h1>Hi</h1>
    </Fragment>);
}
